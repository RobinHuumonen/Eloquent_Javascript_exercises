let triangleCharacter = "#";
while (triangleCharacter.length < 8) {
  console.log(triangleCharacter);
  triangleCharacter += "#"
}

/* Example ans.
for (let line = "#"; line.length < 8; line += "#")
  console.log(line);
*/
